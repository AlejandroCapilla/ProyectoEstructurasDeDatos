package sample;

import javafx.scene.canvas.GraphicsContext;

public class Dibujo {

    void dibujarLinea(GraphicsContext gc) {
        gc.strokeLine(3, 3, 20, 20);
    }

    void dibujarLaberinto(Laberintos l) {
        //Dibujara el laberinto
    }
}
